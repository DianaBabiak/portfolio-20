import { S } from "../HeaderMenu_Styles.ts";
import { HeaderMenu } from "../HeaderMenu.tsx";

export const DesktopMenu = () => {
  return (
    <S.DesktopMenu>
      <HeaderMenu />
    </S.DesktopMenu>
  );
};
